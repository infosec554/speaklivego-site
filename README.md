# Speak Live Go — landing site

Universal one-page marketing site for the Speak Live Go app (Next.js, static export, no backend).

- `npm install`
- `npm run dev` — local
- `npm run build` — static export to `out/` (deploy anywhere: Vercel / Caddy / any static host)
